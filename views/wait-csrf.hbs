<div class="section container">
	<div class="card white section">
		<div class="container">
			<h1 class="center">Approve {{#if item.upper}}{{item.upper}}{{else}}Application{{/if}}?</h1>
			<p class='flow-text center'>If you're sure you want to approve this {{#if item.lower}}{{item.lower}}{{else}}application{{/if}}, click the button below!</p>
			<form method="post" class="center">
				<input type='hidden' name="_csrf" value="{{token}}" />
				<input type='submit' value="Approve!" class="btn green waves-effect waves-light center" />
			</form>
		</div>
	</div>
</div>